# Flask Web框架核心 - Vercel兼容版本
Flask==2.3.3
Werkzeug==2.3.7

# WebSocket支持（仅本地开发）
Flask-SocketIO==5.3.6

# 文档处理
markdown==3.5.1

# HTTP请求
requests==2.31.0

# 文件处理
python-multipart==0.0.6

# Vercel部署优化 - 精简依赖列表
# 移除可能导致问题的依赖：psutil（在Vercel中不可用） 